[generator.lg](../generator.lg)

# filename
- ${formDialogFilename('Show.dialog')}

# template
- ```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.OnAssignEntity",
    "operation": "Show()",
    "actions": [
        {
            "$kind": "Microsoft.SendActivity",
            "activity": "\${propertiesAsActivity('${prefix}', publicProperties())}"           
        }
    ]
    ${designerIntent('Show')}
}
```
