[generator.lg](../generator.lg)

# filename
- ${formFilename('Change.dialog')}

# template
- ```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.OnAssignEntity",
    "operation": "Change()",
    "actions": [
        {
            "$kind": "Microsoft.SetProperty",
            "property": "$PropertyToChange",
            "value": "=turn.dialogEvent.value.property"
        }
    ]
    ${designerName('Change property')}
}
```