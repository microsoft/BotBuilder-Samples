[generator.lg](../generator.lg)

# filename
- ${understandingDir()}${locale}/${prefix}.${locale}.lu

# template
-```
${luHeader()}
${join(foreach(templates.lu, lu, `[${lu.shortName}](${localePath(replace(lu.relative, '\\', '/'))})`), EOL())}
```