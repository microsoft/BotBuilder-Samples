[generator.lg](../generator.lg)

# filename
- ${locale}/${prefix}.${locale}.lu

# template
-```
${luHeader()}
${join(foreach(templates.lu, lu, `[${lu.fullName}](${localePath(lu.relative)})`), EOL())}
```