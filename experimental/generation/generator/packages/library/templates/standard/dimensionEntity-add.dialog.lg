[generator.lg](../generator.lg)

# filename
- ${addFilename('dimension')}

# template
- IF: ${propertySchema.minimum || propertySchema.maximum}
    - ${addWithUnitsAndMinMax('dimension')}
- ELSE:
    - ${addWithUnits('dimension')}
