[generator.lg](../generator.lg)

# filename
- ${formDialogFilename('ChooseProperty.dialog')}

# template
- ```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.OnChooseProperty",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "\${chooseProperties()}"
        }
    ]
    ${designerName('Choose Assignment Prompt')}
}
```
