{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "string",
    "$template": "dynamiclist",
    "$generator": {
        "title": "Dynamic List",
        "description": "A dynamic list is a LUIS list where values and synonyms are defined dynamically at runtime.",
        "allOf": [
            {
                "$ref": "common.json"
            },
            {
                "$ref": "examples.json"
            }
        ]
    }
}