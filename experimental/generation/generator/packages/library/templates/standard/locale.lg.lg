[generator.lg](../generator.lg)

# filename
- ${locale}/${prefix}.${locale}.lg

# template
- ${join(foreach(templates.lg, lg, `[${lg.fullName}](${localePath(replace(lg.relative, '\\', '/'))})`), EOL())}
