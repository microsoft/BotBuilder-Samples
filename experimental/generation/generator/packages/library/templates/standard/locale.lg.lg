[generator.lg](../generator.lg)

# filename
- ${locale}/${prefix}.${locale}.lg

# template
- ${join(foreach(templates.lg, lg, `[${lg.shortName}](${localePath(replace(lg.relative, '\\', '/'))})`), EOL())}
