[generator.lg](../generator.lg)

# filename
- ${addFilename('age')}

# template
- IF: ${propertySchema.minimum || propertySchema.maximum}
    - ${addWithUnitsAndMinMax('age')}
- ELSE:
    - ${addWithUnits('age')}
