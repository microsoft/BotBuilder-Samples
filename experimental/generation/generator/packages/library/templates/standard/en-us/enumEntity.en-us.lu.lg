[standard.en-us.lg](standard.en-us.lg)

# filename
- ${entityLUFilename()}

# template
- ```
> List entity for ${property} enum values
@ list ${templateName(property)}Entity =
${join(foreach(enums(), enum, `    - ${enum}:${EOL()}${synonyms(enum)}`), EOL())}

${operations(`${templateName(property)}Entity`, allSynonyms(), false)}
```

# allSynonyms
- ${flatten(foreach(enums(), enum, phrases(enum)))}

