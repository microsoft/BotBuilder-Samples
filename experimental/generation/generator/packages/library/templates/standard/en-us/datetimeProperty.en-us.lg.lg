[standard.en-us.lg](standard.en-us.lg)

# filename
- ${propertyFilename()}

# template
- ${genericTemplates('date')}${validationTemplate()}

# validationTemplate
- ```
${'#'} ${property}_invalidDate(val)
> Invalid value for '${property}' property
- \${${property}_Value(val)} is not a date```
