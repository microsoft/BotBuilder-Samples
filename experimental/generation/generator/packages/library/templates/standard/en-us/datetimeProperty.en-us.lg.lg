[standard.en-us.lg](standard.en-us.lg)

# filename
- ${propertyFilename()}

# template
- ${genericMessages('date or time expression')}${validationTemplate()}

# validationTemplate
- ```
${'#'} ${templateName(property)}_invalidDate(val)
> Invalid value for '${property}' property
- \${${templateName(property)}_Value(val)} is not a date```
