[standard.en-us.lg](standard.en-us.lg)

# filename
- ${propertyFilename()}

# template
- ${genericTemplates('time')}${validationTemplate()}

> TODO: Move comment before template
# validationTemplate
- ```
${'#'} ${property}_invalidTime(val)
> Value for '${property}' must be a time
- \${${property}_Value(val)} is not a time```
