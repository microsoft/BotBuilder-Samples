[generator.lg](../generator.lg)

# filename
- ${understandingDir()}${locale}/form/${prefix}-custom.${locale}.lu

# template
-```
> Add custom utterances
> Required properties include: ${join(foreach(schema.required, property, property), ', ')}
> Label with: Assign(), Remove(), Help(), Clear(), Show() and Help()
${'#'} ${prefix}
> ADD YOUR LABELS HERE
- ${prefix}
```