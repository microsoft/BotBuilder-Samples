[generator.lg](../generator.lg)

# filename
- ${formDialogFilename('NotUnderstood.dialog')}

# template
- ```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.OnIntent",
    "intent": "None",
    "actions": [
        {
            "$kind": "Microsoft.SendActivity",
            "activity": "\${notUnderstood()}"           
        }
    ]
    ${designerName('Unknown intent')}
}
```
