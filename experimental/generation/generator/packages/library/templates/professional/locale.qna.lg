[generator.lg](../generator.lg)

# filename
- ${knowledgeDir()}${locale}/${prefix}.${locale}.qna

# template
- ${join(foreach(templates.qna, qna, `[${qna.fullName}](${localePath(replace(qna.relative, '\\', '/'))})`), EOL())}
