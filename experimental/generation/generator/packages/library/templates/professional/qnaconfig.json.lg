# filename
- qnaconfig.json

# template
-```
{
    "botName": "${prefix}",
    "out": ".",
    "dialog": "multiLanguage",
    "models": [${join(foreach(locales, locale, `"${locale}/${prefix}.${locale}.qna"`), ', ')}]
}
```