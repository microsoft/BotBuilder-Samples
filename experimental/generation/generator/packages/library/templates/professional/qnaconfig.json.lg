# filename
- qnaconfig.json

# template
-```
{
    "botName": "${prefix}",
    "deleteOldVersion": true, 
    "out": ".",
    "models": [${join(foreach(locales, locale, `"${locale}/${prefix}.${locale}.qna"`), ', ')}]
}
```