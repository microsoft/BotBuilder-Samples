[generator.lg](../generator.lg)

# filename
- config.json

# template
-```
{
${join(foreach(locales, locale, localeTemplate(locale)), ','+EOL())}
}
```

# localeTemplate(locale)
-```    "${prefix}.${locale}": {
            "rootDialog": true,
		    "triggers": {"":""}
        }```

