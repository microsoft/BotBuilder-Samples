[generator.lg](../generator.lg)

# filename
- ${recognizerDir()}${prefix}.qna.dialog

# template
- ```
{
    "$schema": "${appSchema}",
    "id": "QnA_${prefix}",
    "$kind": "Microsoft.MultiLanguageRecognizer",
    "recognizers": ${concat('{', join(foreach(locales, locale, `"${locale}": "${prefix}.${locale}.qna"`), ','), `, "": "${prefix}.${locale}.qna"`, '}')}
}```