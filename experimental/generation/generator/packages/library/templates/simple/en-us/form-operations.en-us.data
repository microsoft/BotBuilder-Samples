>${comment?} The >> is to mark this as the source of entities when doing cross entity examples

>> entity: Standalone entities that map to multiple properties${simple?}
- ${simple*}

>${comment} We ensure that each value occurs at least once here to support reliably resolving entity ambiguity
>> entity: Standalone ${property} with ${entity}
- {${property}=${unique*}}
- {${property}=${uniqueOrValue*} ${name}}

>> property: Standalone ${property} name
- {${property}=${name}}

> Assign() examples for ${property} and ${entity}
- add {${property}=${uniqueOrValue} ${name}}
- with {${property}=${unique}}
- set ${value}
- {${property}=${name} is ${uniqueOrValue}}
- {${property}=${name} of ${uniqueOrValue}}
- {${property}=${name} are ${uniqueOrValue}}

> ${isArray?}Assign() examples with multiple ${entity}
- add {${property}=${uniqueOrValue} and ${uniqueOrValue} ${name}}
- set {${property}=${unique} and ${unique}}
- with ${value} and ${value}
- with {${property}=${uniqueOrValue}, ${uniqueOrValue} and ${uniqueOrValue} ${name}}
- add {${property}=${unique}, ${unique} and ${unique}} 
- set ${value}, ${value} and ${value}
- {${property}=${name} are ${uniqueOrValue} and ${uniqueOrValue}}
- {${property}=${name} of ${uniqueOrValue}, ${uniqueOrValue} and ${uniqueOrValue}}
