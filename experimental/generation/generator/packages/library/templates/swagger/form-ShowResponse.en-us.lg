# showResponse(response)
> Show http response activity
[Activity
    Text = ${showResponse_text(response)}
]

# showResponse_text(response)
> Show http response
- IF : ${response.statusCode == 200}
- The response is ${response} .
- ELSE : 
- The response code is ${response.statusCode} . 