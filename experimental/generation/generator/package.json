{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "clean": "lerna run clean",
    "test": "lerna run build && lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
