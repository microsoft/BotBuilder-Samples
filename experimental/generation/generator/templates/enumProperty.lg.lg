[generator.lg](generator.lg)

# filename
- ${propertyFilename()}

# template
- ```
${importLG(`${property}Entity`)}
${importLG('library')}
${importLG('library-Missing')}
${AskTemplate()}
${nameTemplate()}
${ValueTemplate()}
```

# AskTemplate
- ```
${'#'} Ask${property}
- \${askEnum('${property}')}
```

# ValueTemplate
- ```
${'#'} ${property}(val)
- \${${property}Entity(val)}
```


