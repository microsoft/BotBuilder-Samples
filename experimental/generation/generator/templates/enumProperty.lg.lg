# filename
- ${propertyFilename()}

# template
- ```
${AskTemplate()}
${nameTemplate()}
${ValueTemplate()}

${importLG(`${property}Entity`)}
${importLG('library')}
${importLG('library-Missing')}
```

# AskTemplate
- ```
# Ask${property}
- \${askEnum('${property}')\}
```

# ValueTemplate
- ```
# ${property}(val)
- \${${property}Entity(val)}
```


[generator.lg](generator.lg)