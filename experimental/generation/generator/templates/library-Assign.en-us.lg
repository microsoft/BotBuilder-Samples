[library.en-us.lg]

# getNumberValidatationText(property, number)
- IF: ${less(number, dialogClass.schema.properties[property].minimum)}
- ${number} is less than the minimum value ${dialogClass.schema.properties[property].minimum} for ${getPropertyName(property)}.
- ELSEIF: ${greater(number, dialogClass.schema.properties[property].maximum)}
- ${number} is greater than the maximum value ${dialogClass.schema.properties[property].maximum} for ${getPropertyName(property)}.
- ELSE:
-

# getUnitsAlertText(property)
- IF: ${dialogClass.schema.properties[property].$units}
- Required response for ${getPropertyName(property)} should be in ${dialogClass.schema.properties[property].$units}.
- ELSE:
- Unit for ${getPropertyName(property)} are not defined in the schema.

# getSetObjectPropertyText(property, val)
- IF: ${contains($expectedProperties, property)}
- 
- ELSEIF: ${dialog[property]} 
- ${getPropertyName(property)} is changed from ${getPropertyValueText(property, dialog[property])} to ${getPropertyValueText(property, val)}.
- ELSE: 
- ${getPropertyName(property)} is set as ${getPropertyValueText(property, val)}.

# getSetPropertyText(property, val)
- IF: ${contains($expectedProperties, property)}
- 
- ELSE:
- ${getSetUnexpectedPropertyText(property, val)}

# getSetUnexpectedPropertyText(property, val)
- IF:${dialog[property]}
- ${getSetUnexpectedPropertyChangedText(property, val, dialog[property])}
- ELSE:
- ${getPropertyName(property)} is set to ${getPropertyValueText(property, val)}.

# getSetUnexpectedPropertyChangedText(property, val, oldVal)
- ${getPropertyName(property)} is changed from ${getPropertyValueText(property, oldVal)} to ${getPropertyValueText(property, val)}.

# getSetArrayPropertyText(property, val)
- IF: ${contains($expectedProperties, property)}
- 
- ELSE:
- Added ${getPropertyValueText(property, val)} to ${getPropertyName(property)}

# getClearPropertyText(property)
- ${getPropertyName(property)} has been cleared.

# getRemovePropertyText(property, val)
- IF: ${contains($expectedProperties, property) && startsWith($expectedOperation, 'RemoveFrom')}
- 
- ELSE:
- ${getRemoveUnexpectedPropertyText(property, val)}

# getRemoveUnexpectedPropertyText(property, val)
- Removed ${getPropertyValueText(property, val)} from ${getPropertyName(property)}.

# getRemoveArrayPropertyText(property, val)
- IF: ${contains($expectedProperties, property) && startsWith($expectedOperation, 'RemoveFrom')}
- 
- ELSE:
- ${getRemoveUnexpectedArrayPropertyText(property, val)}

# getRemoveUnexpectedArrayPropertyText(property, val)
- IF: ${contains(dialog[property], val)}
- Removed ${getPropertyValueText(property, val)} from ${getPropertyName(property)}
- ELSE:
- ${getPropertyValueText(property, val)} is not in ${getPropertyName(property)}

# getShowPropertyText(property)
- ${getPropertyName(property)}: ${getPropertyValueText(property, dialog[property])}

