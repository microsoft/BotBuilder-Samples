# filename
- ${prefix}.${locale}.lu.dialog

# template
-```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.LuisRecognizer",
    "applicationId": "=settings.luis.${prefix}_${replace(locale, '-', '_')}_lu",
    "endpoint": "=settings.luis.endpoint",
    "endpointKey": "=settings.luis.endpointKey",
    "predictionOptions": {
        "includeInstanceData": true
    }
}
```
