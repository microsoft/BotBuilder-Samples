[generator.lg](generator.lg)

# filename
- ${propertyFilename()}

# template
- ```
${importLG(`${property}Entity`)}
${importLG('library')}
${importLG('library_Missing')}
${AskTemplate()}
${nameTemplate()}
${valueTemplate()}
```

# AskTemplate
- ```
${'#'} Ask${property}
- \${askString('${property}')}
```
