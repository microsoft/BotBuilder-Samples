[generator.lg](generator.lg)

# template
- ```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.OnChooseProperty",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "\${getChoosePropertiesText()}",
            "expectedProperties": ["PropertyToChange"]
        }
    ]
    ${designerChooseProperty('PropertyToChange')}
}
```
