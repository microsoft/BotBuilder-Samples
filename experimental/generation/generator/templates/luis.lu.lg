# filename
- luis/${prefix}.${locale}.lu.dialog

# template
-```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.LuisRecognizer",
    "applicationId": "=settings.luis.${prefix}_${locale}_lu",
    "endpoint": "=settings.luis.endpoint",
    "endpointKey": "=settings.luis.endpointKey",
    "predictionOptions": {
        "includeInstanceData": true
    }
}
```

