[generator.lg](generator.lg)

# filename
- ${chooseEntityFilename()}

# template
- ```
{
    "$schema": "${appSchema}",
    "$kind": "Microsoft.OnChooseEntity",
    "entity": "${property}Entity",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "\${getChooseEntity_${property}Entity()}",
            "expectedProperties": ["${property}"]
        }
    ]
    ${designerChooseEntity()}
}
```
