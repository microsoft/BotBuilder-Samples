[generator.lg](generator.lg)

# filename
- ${locale}/${prefix}.${locale}.lu

# template
-```
${luHeader()}
${join(foreach(templates.lu, lu, `[${lu.fullName}](../${locale}/${lu.fullName})`), EOL())}
```

