[generator.lg](generator.lg)

# filename
- ${entityLGFilename()}

# template
-```
${importLG('library')}
${importLG('library-Choose')}
${propertyEntity()}
${chooseEntity()}
```

# chooseEntity
-```
${'#'} getChooseEntityText_${property}Entity
- \${getChooseEnumEntityText(${concat("'", property, "'")})}
```

# propertyEntity
- ${join([namePlusSwitch(), cases(), default()], '')}

# namePlusSwitch
-```
${'#'} getEntityValueText_${property}Entity(value) 
- SWITCH: \${value}```

# case(option)
-```
  - CASE: \${'${option}'} 
    - ${phrase(option)}```

# cases
- ${join(foreach(schema.properties[property].enum, enum, case(enum)), '')}

# default
-```
  - DEFAULT: 
    - no value
```

