# ViewEntries
- ```
You have @{count(user.Entries)} @{lgTemplate("Items-Ordinality")} in your calendar.
- @{join(user.Entries, '\r\n- ')}
@{lgTemplate("Welcome-Actions")}
```

# NoEntries
-```
You do not have any entries in your calendar.
@{lgTemplate("Welcome-Actions")}
```

# Items-Ordinality
- IF: {user.Entries != null && count(user.Entries) == 1}
    - item
- ELSE:
    - items