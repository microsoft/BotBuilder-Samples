# NameReadBack
- IF : ${exists(user.profile.name)}
    - Name : ${user.profile.name}
- ELSE :
    - Name : unknown

# AgeReadBack
- IF : ${exists(user.profile.age)}
    - Age : ${user.profile.age}
- ELSE:
    - Age : unknown
