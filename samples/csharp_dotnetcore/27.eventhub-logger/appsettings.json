{
  "MicrosoftAppId": "",
  "MicrosoftAppPassword": "",

  "ApplicationInsights": {
    // Specifies my instrumentation key for Application Insights.
    "InstrumentationKey": "10101010-1010-1010-1010-101010101010"
  },

  // Specifies the connection string for the event hub.
  // The corresponding shared access policy must have send permission.
  "eventHub.connectionString": "Endpoint=sb://*.servicebus.windows.net/;SharedAccessKeyName=*;SharedAccessKey=*",

  // Specifies the name of the event hub.
  "eventHub.name": "myEventHub",

  // Specifies the maximum time that events are held in a batch before the batch gets sent.
  // The batch is sent when it reaches the max event data batch size, or its oldest event has been
  // in the batch for more than eventhub.batchDelaySec seconds.
  "eventhub.batchDelaySec": 10
}